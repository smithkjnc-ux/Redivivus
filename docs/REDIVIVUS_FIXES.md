# Redivivus Fixes
> Log every file change here. See REDIVIVUS_ROADMAP.md for index.

**2026-06-27 — Wiring gate blind spot: ./src/ paths also wrong under non-default Vite root**
- **Root cause**: `_hasAboveRootLinks` only checked for `../` in HTML link/script paths. After a partial fix changed `../src/` → `./src/`, the gate stopped firing. But `./src/styles/global.css` inside `public/index.html` with `root: 'public'` also 404s — Vite looks for `public/src/styles/global.css` which doesn't exist.
- **Fix**: Expanded regex to also match `./src/`, `./styles/`, `./js/`, `./css/`, `./assets/` — common source directories at project root that would be wrong inside a non-default Vite root subfolder. Gate still requires `_hasNonDefaultViteRoot` so it won't fire for `root: '.'` projects.
- **File**: `src/features/fix/chatPanelMsgFix.ts` line 82

**2026-06-27 — PRESCRIPTION_CHECK false-positive retry trigger fixed**
- **Root cause (from log)**: `PRESCRIPTION_CHECK` in `chatPanelMsgFixPhase23.ts` lines 79–84 extracted every filename mentioned anywhere in the prescription text — including CSS files cited as path-references, not as files that needed writing. Condition `unwrittenPrescribed.length > 0` triggered when SOME mentioned files weren't written to disk, even though the files that WERE written (vite.config.js, public/index.html) were correct. This nulled `written = []` and discarded a successful fix, launching a second escalation loop against already-modified files → "Search block not found" × 3 → Supervisor self-fix → HTML written to vite.config.js via legacy parser fallback.
- **Fix**: Changed condition to `unwrittenPrescribed.length === prescribedFiles.length` — only force retry when ALL prescribed files were unwritten (Worker fixed completely wrong file). If some were written, the fix was on-target and the partial miss is logged as info only.
- **File**: `src/features/fix/chatPanelMsgFixPhase23.ts` lines 79–84

**2026-06-27 — Static Gate dry-run fix: eliminate disk-mutation cascade in escalation loop**
- **Root cause**: `runStaticCompilationGateForFix` in `src/features/build/chatPanelBuildReview.ts` was writing surgical edits to disk, running a compile check, then reverting via `try { fs.writeFileSync(...) } catch {}`. The silent catch caused the revert to fail without trace — leaving files mutated (e.g. `root: 'public'` → `root: '.'`). Every subsequent retry in the escalation loop then searched for the original text and reported "Search block not found", generating false `[COMPILATION ERROR]` critiques that burned 2–3 retry slots for zero gain.
- **Fix**: Replaced write→compile→revert with an in-memory dry-run for the search-block presence check: reads each file, checks if `searchBlock` (exact + whitespace-normalized) exists — returns error immediately if not, with zero disk mutation. Real write→compile→revert is now only triggered for TypeScript files (where `tsc` produces useful output) and uses `fs.writeFileSync` without a silent catch so revert failures are visible.
- **File**: `src/features/build/chatPanelBuildReview.ts` — `runStaticCompilationGateForFix` (~49 lines, rewrote the function body)

**2026-06-26 — Hybrid Architecture: ai promotion**
- **`src/features/ai/`**: Promoted from `shared/ai/`. 91 files. Layer split: `domain/` (20 files) + `domain/providers/` (9 files, subfolder preserved) → `logic/`; `infrastructure/` (62 files) → `data/`. No depth changes — all layers at depth 3/4 in both source and target. Fixed 4 internal cross-layer ref patterns: `'../infrastructure/` → `'../data/` (logic/ files); `'../../infrastructure/` → `'../../data/` (logic/providers/ files); `'../domain/providers/` → `'../logic/providers/` (data/ files); `'../domain/` → `'../logic/` (data/ files — dynamic import in routingServiceSupervisor.ts was missed by first pass, caught by TSC). Broad 3-way sweep (providers/ before domain/ to avoid double-match). All `'../../../features/` refs were already correct from prior migrations. TSC: 0 errors. `shared/` now contains only `shared/ui/` — blueprint target state reached.

**2026-06-26 — Hybrid Architecture: vscode promotion**
- **`src/features/vscode/`**: Promoted from `shared/vscode/`. 25 files across 5 subdirs. Layer split: `application/` (7) + `domain/rules/` (5) + `misc*.ts` (4 root files) → `logic/` (preserving `rules/` subfolder); `ui/` (6) + `ui/sidebar/` (3) → `ui/` (preserving `sidebar/` subfolder). No net depth changes except `misc*.ts` which went from depth 2 → depth 3 (required special transforms: `'./application/' → './'`, `'./domain/rules/' → './rules/'`, `'./ui/' → '../ui/'`, `'../../features/' → '../../'`). Fixed cross-layer refs: `'../domain/rules/' → './rules/'` (redivivusService.ts); `'../../application/' → '../'` (rulesService.ts); `'../application/' → '../logic/'` (ui/ files); `'../../application/' → '../../logic/'` (sidebar/ files). Fixed shared sibling refs in logic/ + ui/: `'../../ai/infrastructure/' → '../../../shared/ai/infrastructure/'`, `'../../ui/styles.js' → '../../../shared/ui/styles.js'`. Pattern groups A–E applied by depth. Broad 5-way sweep updated all `extension*.ts`, `features/*/`. Second sweep caught `shared/ai/` using `'../../vscode/application/' → '../../../features/vscode/logic/'` (5 files). TSC: 0 errors.

**2026-06-26 — Hybrid Architecture: api promotion**
- **`src/features/api/`**: Promoted from `shared/api/`. Layer split: `infrastructure/` → `data/` (apiClient, apiClientChat, apiClientKnowledge, apiClientTelemetry — 4 raw HTTP callers); `infrastructure/authHandler.ts` → `logic/` (auth orchestration, not raw I/O); `mcp/` (mcpService, mcpServiceTypes) → `logic/`. 7 files total. No depth change. Fixed `authHandler.ts`: `'./apiClient.js'` → `'../data/apiClient.js'`; `'../../vscode/ui/statusBar.js'` → `'../../../shared/vscode/ui/statusBar.js'`. Fixed `apiClient.ts`: `'../../ai/infrastructure/routingKeys.js'` → `'../../../shared/ai/infrastructure/routingKeys.js'`. Broad 2-way sweep updated `extension*.ts`, `features/build/`, `features/chat/`, `features/onboarding/`, `features/vault/`. Second sweep caught `shared/ai/` + `shared/vscode/` sibling refs `'../../api/infrastructure/` + `'../../api/mcp/` → `'../../../features/api/data/` + `'../../../features/api/logic/`. Post-sweep fix: `extensionCommands.ts` had `authHandler` in `data/` from broad sweep — corrected to `logic/`. TSC: 0 errors.

**2026-06-26 — Hybrid Architecture: logging promotion**
- **`src/features/logging/`**: Promoted from `shared/logging/`. Layer split: `application/logging.ts` + `domain/masterLogger.ts` → `logic/` (2 files); `infrastructure/` (6 files: fixPipelineLogger, logListeners, projectContextLogger, redivivusLoggerOps, redivivusLogger, redivivusLogging) → `data/`; `ui/outputChannelManager.ts` → `ui/`. No depth change — all layers at depth 3. Fixed internal cross-layer imports: logic/ + ui/ files updated `'../infrastructure/` → `'../data/`. Fixed `logging.ts` path corrections: `'../../vscode/application/` → `'../../../shared/vscode/application/` (shared/vscode/ not yet promoted); `'../../../features/chat/ui/` → `'../../chat/ui/`. Fixed `redivivusLogging.ts`: `'../../../features/project/` → `'../../project/`. Broad sweep (4-way) updated all external callers in `extension*.ts`, `features/*/`, `tests/`. Second sweep caught `shared/ai/` + `shared/vscode/` using sibling relative paths `'../../logging/X/` — updated to `'../../../features/logging/X/`. TSC: 0 errors.

**2026-06-26 — Hybrid Architecture: chat migration**
- **`src/features/chat/`**: `application/` (5 files) + `routing/` (49 files) → `logic/`. `ui/` unchanged. No depth change — all layers at depth 3. Fixed internal imports: logic/ files with `'../application/` → `'./`; ui/ files with `'../routing/` + `'../application/` → `'../logic/` (including dynamic `require()` calls in chatPanel.ts and memoryPanel.ts). Broad sweep updated `extension*.ts`, `features/build/`, `features/fix/`, `features/project/`, `features/workspace/`, `shared/ai/`, `shared/vscode/`. TSC: 0 errors.

**2026-06-26 — Hybrid Architecture: fix carve-out**
- **`src/features/fix/`**: Carved out from `chat/routing/` (26 `chatPanelMsgFix*` + `fixActivityPanel.ts`) and `chat/ui/` (`fixProgressStyle.ts` — exclusively used by fix files). 28 files total, depth 3→2. Fixed internal imports: `'../ui/fixProgressStyle.js'` → `'./fixProgressStyle.js'`, `'./chatPanelMessages.js'` → `'../chat/routing/chatPanelMessages.js'`, `'../application/` → `'../chat/application/`, `'../ui/buildActivity/` → `'../chat/ui/buildActivity/`, `'../../build/` → `'../build/`, `'../../project/` → `'../project/`, `'../../workspace/` → `'../workspace/`, `'../../../shared/` → `'../../shared/`. Broad sweep updated `features/build/` and `shared/ai/`. Second sweep caught non-fix `chat/routing/` files using `'./chatPanelMsgFix.js'` → `'../../fix/chatPanelMsgFix.js'`. Third sweep caught `chat/ui/chatPanelMsgArchitect*.ts` using `'../routing/chatPanelMsgFix.js'` → `'../../fix/chatPanelMsgFix.js'`. `fileSizeGate.ts` left in `chat/routing/` (also used by `chatPanelMsgTools.ts`); fix files updated to `'../chat/routing/fileSizeGate.js'`. TSC: 0 errors.

**2026-06-26 — Hybrid Architecture: build carve-out**
- **`src/features/build/`**: Carved out from `chat/build/` — 77 files (40 root + 37 services/). Moved flat, preserving `services/` subfolder. Depth changes: root 3→2 (lost one `../`), services 4→3. Fixed root file imports: `'../ui/` + `'../routing/` + `'../application/` → `'../chat/...`, all `'../../feature/` → `'../feature/`, `'../../../` → `'../../`. Fixed services imports: `'../../routing/` → `'../../chat/routing/`, `'../../../feature/` → `'../../feature/`, `'../../../../` → `'../../../`. Broad sweep caught external callers in `extension*.ts`, `features/map/`, `features/project/`, `features/workspace/`, `shared/ai/`. Second sweep caught `chat/routing/` and `chat/ui/` which used `'../build/` sibling paths — updated to `'../../build/`. TSC: 0 errors.

**2026-06-26 — Hybrid Architecture: blueprint promotion**
- **`src/features/blueprint/`**: Promoted from `project/data/blueprint/` (18 files) + `project/ui/blueprint/` (4 files). Layer split: 16 files → `logic/` (orchestration, inference, interview, services), 2 files → `data/` (blueprintWriter, blueprintRevisions — file I/O), 4 files → `ui/`. `project/data/` and `project/ui/blueprint/` directories removed. Fixed depth regression (`../../../../` → `../../../`) across all promoted files. Fixed 3 internal logic→data cross-layer imports (`./blueprintWriter.js` → `../data/blueprintWriter.js`, `./blueprintRevisions.js` → `../data/blueprintRevisions.js`). Fixed `ui/blueprintInterviewPanel.ts` → `../logic/blueprintInterview.js`. Fixed `project/logic/blueprint.ts` cross-feature paths. Broad sweep updated `extension*.ts`, `features/chat/`, `shared/ai/`. Second sweep caught `project/ui/blueprint/` callers missed by first pass (`extensionCommands.ts`, `extensionInlineCommands*.ts`, `chat/routing/`, `chat/ui/`, `project/logic/retrofitBlueprint.ts`). TSC: 0 errors.

**2026-06-26 — Hybrid Architecture: project migration**
- **`src/features/project/`**: `application/` → `logic/` (41 files). `domain/` root → `logic/` (12 files). `domain/inspector/` → `logic/inspector/` (5 files, subfolder preserved). `domain/retrofit/` → `logic/retrofit/` (6 files, subfolder preserved). `infrastructure/blueprint/` → `data/blueprint/` (18 files). No depth changes — all moves were layer-to-layer at same depth. Fixed internal imports: logic/ files updated `../infrastructure/blueprint/` → `../data/blueprint/`, `../domain/` → `./`, `../application/` → `./`; ui/ files updated `../../infrastructure/blueprint/` → `../../data/blueprint/`, `../../application/` → `../../logic/`. Broad sweep updated all external callers in `extension*.ts`, `shared/ai/`, `shared/vscode/`, `shared/logging/`. TSC: 0 errors.

**2026-06-26 — Hybrid Architecture: vault migration**
- **`src/features/vault/`**: `application/` → `logic/` (7 files). `domain/` → `logic/` (4 files). `infrastructure/` → `data/` (21 files). Fixed internal imports in all `logic/` files (`../infrastructure/` → `../data/`) and all `ui/` files (`../infrastructure/` → `../data/`, `../domain/` → `../logic/`). Broad sweep updated external callers in `extension*.ts`, `shared/ai/`, `shared/vscode/`. TSC: 0 errors.

**2026-06-26 — Hybrid Architecture: workspace migration**
- **`src/features/workspace/`**: `application/` → `logic/` (12 files). `domain/` → `logic/` (5 files). `domain/code/` → `logic/` (10 files, flattened — `code/` subfolder removed). `infrastructure/` → `data/` (13 files). `ui/analyzer/` split: analyzerService, analyzerLogger, analyzerReports, analyzerScanner, analyzerTypes → `logic/`; 6 UI files (panel, html, script, scriptHead, sections, styles) stay in `ui/analyzer/`.
- Fixed internal cross-layer imports: githubBackup, scopeCreep, duplicateCode, analysis, lensService, analyzerService (6 files). Fixed depth regression on 7 files moved from 4-level-deep paths (`domain/code/`, `ui/analyzer/`) to 3-level `logic/` (`../../../../` → `../../../shared/`; `../../../chat/` → `../../chat/`). Fixed `analyzerPanel` and `analyzerSections` in `ui/` to reference `../../logic/analyzerTypes.js`. Broad sed sweep updated ~30 external callers across `extension*.ts`, `features/chat/`, `features/project/`, `features/vault/`, `features/onboarding/`, `shared/ai/`. TSC: 0 errors.

**2026-06-26 — Hybrid Architecture: onboarding migration**
- **`src/features/onboarding/`**: Split `application/` into `ui/` + `logic/`. UI files (apiSetupHtml, apiSetupHtmlCards, apiSetupScript, apiSetupStyles, setupHubHtml) → `ui/`. Logic files (apiSetup, apiSetupPanel, setupHub, setupProgressCommand, signIn) → `logic/`. Fixed 2 internal cross-layer imports (`apiSetupPanel` and `setupHub` now reference `../ui/`). Fixed 5 external imports in `extensionCommands.ts` and `extensionInlineCommands.ts`. TSC: 0 errors.

**2026-06-26 — Hybrid Architecture: runtime + map migration**
- **`src/features/runtime/`**: `application/` → `logic/` (profileRuntime, startRuntimeAnalysis, startRuntimeAnalysisHelpers). `infrastructure/` → `data/` (runtimeProfiler, runtimeProfilerDetect, runtimeProfilerIpc, runtimeProfilerScan, runtimeLogger). Absorbed orphaned `src/runtime/pythonInstrumentor.ts` and `jsInstrumentor.ts` into `data/` — `src/runtime/` directory deleted. Fixed internal imports in logic/ files and external callers in `extensionCommands.ts`, `extensionInlineCommands.ts`, `extensionInlineCommandsB.ts`.
- **`src/features/map/`**: `application/` → `logic/` (mapBuilderService, mapBuilderHelpers). Fixed imports in 5 `ui/` files (mapPanel, mapMessageDispatcher, mapPanelHtml, mapTab, commands/architectReviewCommand) and 1 cross-feature caller (`features/project/domain/chatPanelProjectContext.ts`). TSC: 0 errors.

**2026-06-26 — Hybrid Architecture: pwa + settings migration**
- **`src/features/pwa/`**: `application/` → `logic/` (addToPhoneCommand). `infrastructure/` → `data/` (pngIcon, pwaGenerator, pwaPublish, pwaTemplates). Fixed internal imports in `addToPhoneCommand.ts`, `addToPhonePanel.ts`, `addToPhonePanelHtml.ts`. Fixed external import in `extension.ts`.
- **`src/features/settings/`**: `application/` → `logic/` (checkForUpdates, configureEditorRules, personalityPicker). Fixed external imports in `extensionCommands.ts`, `extension.ts`, `shared/vscode/misc.ts`, `chat/routing/chatPanelMessageRouter.ts`, `chat/routing/chatPanelMsgSendPostCloud.ts`. TSC: 0 errors.

**2026-06-26 — Hybrid Architecture: blueprint + telemetry PoC migration**
- **`.redivivus/blueprint.md`**: Fully rewrote to document the Hybrid Feature-Slice + Layer architecture. Defines `ui/logic/data` layer contract, feature registry (17 features), dependency rules, and migration status tracker.
- **`src/features/telemetry/`**: Proof-of-concept migration to `ui/logic/data` layers. `application/` → `logic/` (usageCommands, usageFormatters). `infrastructure/` → `data/` (usageTracker, usageTrackerReport, usageCosts). Empty old dirs removed.
- **Import sweep**: Updated all callers across `extensionCommands.ts`, `extensionInlineCommands*.ts`, `extensionResumeState.ts`, `extensionServices.ts`, `shared/ai/domain/chatPanelBuildDeps.ts`, `shared/vscode/ui/statusBar.ts`, and 20+ files in `features/chat/`, `features/runtime/`, `features/workspace/`, `features/vault/`. TSC: 0 errors.

- **`src/features/chat/build/chatPanelEditBuild.ts`**: Extracted prompt generation into `chatPanelEditBuildPrompts.ts` to enforce Rule 9.
- **`src/features/chat/build/services/cloudBuildMultiFile.ts`**: Extracted fallback logic to `cloudBuildProviderFallback.ts` and Guardian step logic to `cloudBuildMultiFileHelpers.ts` to enforce Rule 9.
- **`src/features/chat/build/services/smokeTestGenerator.ts`**: Extracted helpers to `smokeTestHelpers.ts` to enforce Rule 9.
- **`src/features/chat/routing/chatPanelMessages.ts`**: Extracted architect router to `chatPanelMsgArchitectRouter.ts` to enforce Rule 9.
- **`src/features/chat/routing/chatPanelMsgSendAI.ts`**: Extracted Guardian and auto-save logics into `chatPanelMsgSendAIHelpers.ts` to enforce Rule 9.
- **`src/features/chat/ui/memoryPanelHtml.ts`**: Extracted CSS and JS strings into `memoryPanelHtmlStyles.ts` and `memoryPanelHtmlScripts.ts` to enforce Rule 9.
- **`src/shared/ai/domain/chatPanelAI.ts`**: Extracted code-generation prompts into `chatPanelAICodeGen.ts` to enforce Rule 9.
- **`src/shared/vscode/ui/filesTab.ts`**: Extracted switch form logic to `filesTabForms.ts` to enforce Rule 9.
- **`src/` (Global Phase 1, 2, 3)**: Fully migrated extension to the Hybrid Modular Monolith Architecture. 
  - Phase 1: Moved 407 primary files into `features/` (`chat`, `vault`, `project`, `workspace`, `onboarding`) and `shared/`.
  - Phase 2: Dissolved legacy `src/commands` bucket into 37 feature-specific bounded contexts (`telemetry`, `runtime`, `settings`).
  - Phase 3: Dissolved the final 129 legacy files from `src/services`, `src/ui`, and `src/core`. Established new boundaries for `features/map/`, `features/pwa/`, `shared/vscode/`, and `shared/api/`.
  - Fully automated cross-boundary TS module pathing and `import()` / `require()` dynamic path patching using bidirectional AST `ts-morph` traversals.
- **`src/features/workspace/domain/selfDiagnosticBuildChecks.ts`**: Patched `__dirname` relative path lookups to point to new `shared/ai/infrastructure` paths for `routingService.js` and `guardianAI.js`.
- **`src/lib/ai/routingServicePrompts.ts`**: (Redivivus Backend) Updated `generateSupervisorPrompt` to mandate a Bottom-Up Build Sequence (leaf dependencies first, entry points last). This ensures the AI Worker has exact, previously generated API signatures when building components that rely on them. Added strict mandates for JSDoc parameter documentation and explicitly typing constructor/method contracts in the JSON plan.
- **`/home/papajoe/projects/games/fluid-dynamics-sandbox/`**: (Test Sandbox) Fixed API mismatch between `physics.js` and `spatialHashGrid.js` caused by top-down generation order. Restructured `input.js` to return a state object. Dropped particle count to 300 and hardcoded a 3-update circuit breaker in `main.js` to stop the 2-FPS Spiral of Death caused by slow collision checking.

- **`src/core/routing/chatPanelMsgFixEscalation.ts`**: Refactored monolithic file (324 lines) into 4 separate files to comply with Rule 9. Extracted `WorkerPhase`, `VerifyPhase`, and `GuardianPhase` logics into `chatPanelMsgFixWorkerPhase.ts`, `chatPanelMsgFixVerifyPhase.ts`, and `chatPanelMsgFixGuardianPhase.ts` respectively.
- **`src/core/routing/chatPanelMsgFixWorkerPhase.ts`**: Created extracted Worker generation logic for escalation loop.
- **`src/core/routing/chatPanelMsgFixVerifyPhase.ts`**: Created extracted Verify Phase logic for escalation loop.
- **`src/core/routing/chatPanelMsgFixGuardianPhase.ts`**: Created extracted Guardian Phase logic for escalation loop.
- **`src/core/routing/chatPanelMsgFixSelfFix.ts`**: Fixed escaped template literal syntax error from generation.
